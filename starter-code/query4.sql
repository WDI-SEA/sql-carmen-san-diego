
SELECT * FROM city WHERE countrycode = 'SMR';

/* This query gives two names back Serravalle and San Marino. 
Since it's not San Marino (because that is the same as the country name), the answer is Serravalle. */